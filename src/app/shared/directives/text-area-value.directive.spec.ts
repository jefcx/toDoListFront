import { TextAreaValueDirective } from './text-area-value.directive';

describe('TextAreaValueDirective', () => {
  it('should create an instance', () => {
    const directive = new TextAreaValueDirective();
    expect(directive).toBeTruthy();
  });
});
